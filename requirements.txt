# ─────────────────────────────────────────────────────────────────
# requirements.txt — Root-Level Dependencies
# PneumoCloud AI | Multi-Cloud Pneumonia Detection System
#
# These packages are required to run the Streamlit dashboard locally.
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# Frontend dashboard
streamlit>=1.32.0

# HTTP requests (used by dashboard to call GCP Cloud Function)
requests>=2.31.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0

# AI / ML (needed for Local Model mode)
tensorflow>=2.15.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# AWS SDK (needed for local testing of Lambda)
boto3>=1.34.0

# Plotting
matplotlib>=3.7.0
